name: Build macOSX

on:
  push:
    # not specifying any branches means triggering on all of them
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: macos-10.15

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Test MATLAB
      run: |
        ls

